"use client";
import React from "react";

const AddChart = () => {
  return (
    <div>
      <button onClick={() => console.log("object")}>Add to chart</button>
    </div>
  );
};

export default AddChart;
